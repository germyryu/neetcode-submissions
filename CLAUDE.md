# CLAUDE.md

## Language

All code and pseudocode in responses: **Rust**. No exceptions, even for explanations — use real or Rust-flavored pseudocode (`fn`, `let`, `match`, etc.), not Python/JS/C++.

## Repo

NeetCode solutions, done as Rust practice. Layout: `Data Structures & Algorithms/<problem-name>/submission-N.rs`, N increments per new attempt on same problem — don't overwrite prior submissions, add a new numbered file.
